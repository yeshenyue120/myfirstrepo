# ===== Docker Compose 环境变量（示例）=====
# 复制为 .env 并填写真实值。.env 已被 .gitignore 排除，不会进代码库。
# 生产部署请务必替换 JWT_SECRET 和 MAIL_*（见根 README）。

# 数据库
DB_PASSWORD=123456

# 邮件（QQ 邮箱 SMTP 示例）
MAIL_HOST=smtp.qq.com
MAIL_PORT=465
MAIL_USERNAME=your_email@qq.com
MAIL_PASSWORD=your_smtp_auth_code   # SMTP 授权码，不是登录密码

# JWT 密钥（生产必须换长随机串，如 $(openssl rand -base64 48)）
JWT_SECRET=calorie-tracker-dev-secret-key-0123456789abcdef0123456789abcdef

# 允许跨域的前端 Origin（逗号分隔）。生产示例：
# CORS_ALLOWED_ORIGINS=http://211.149.250.23:8081
# CORS_ALLOWED_ORIGINS=http://localhost:*,http://127.0.0.1:*

# 前端访问地址（邮件重置链接用）。生产示例：
# APP_FRONTEND_URL=http://211.149.250.23:8081